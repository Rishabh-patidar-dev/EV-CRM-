// Shared localStorage key for the Spare Part Returns sidebar asterisk
// (components/Sidebar.tsx) and the page that clears it
// (app/(dashboard)/inventory-management/returns/page.tsx). Same pattern as
// warrantyClaimsSeen.ts's WARRANTY_LAST_SEEN_KEY.
export const RETURNS_LAST_SEEN_KEY = "spare-part-returns:lastSeenAt";
