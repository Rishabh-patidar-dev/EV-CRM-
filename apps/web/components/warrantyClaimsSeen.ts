// Shared localStorage key for the Warranty Management sidebar asterisk
// (components/Sidebar.tsx) and the page that clears it
// (app/(dashboard)/warranty/page.tsx). Same pattern as
// orderManagementSeen.ts's ORDER_MGMT_LAST_SEEN_KEY.
export const WARRANTY_LAST_SEEN_KEY = "warranty:lastSeenAt";
