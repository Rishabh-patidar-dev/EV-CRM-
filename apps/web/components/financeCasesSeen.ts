// Shared localStorage key for the Finance Management sidebar asterisk
// (components/Sidebar.tsx) and the page that clears it
// (app/(dashboard)/finance-management/page.tsx). Same pattern as
// warrantyClaimsSeen.ts's WARRANTY_LAST_SEEN_KEY.
export const FINANCE_LAST_SEEN_KEY = "finance-cases:lastSeenAt";
